use crate::day_04::ast::*;
use std::str::FromStr;

grammar;

pub Cards: Vec<Card> = {
    Card* => <>
}

Card: Card = {
    "Card" <i:Num> ":" <w:Num+> "|" <o:Num+> => Card(i, w, o),
}

Num: u32 = {
    r"[0-9]+" => u32::from_str(<>).unwrap()
};
